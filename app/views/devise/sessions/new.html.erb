<div class="sign-in-page">
  <div class="card">
    <div class="card-header">
      ログインする
    </div>

    <div class="card-body">
      <%= form_with scope: resource, as: resource_name, url: session_path(resource_name) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="mb-2">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "メールアドレス" %>
        </div>

        <div class="mb-2">
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "パスワード" %>
        </div>

        <div class="sign-in__submit-btn d-grid">
          <%= f.submit "ログイン", class: "btn text-white" %>
        </div>
      <% end %>

      <div class="mt-3">
        <%= render "devise/shared/links" %>
      </div>
    </div>

  </div>
</div>
